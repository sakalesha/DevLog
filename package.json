{
    "name": "devlog-monorepo",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "build": "cd client && npm install && npm run build",
        "postinstall": "cd server && npm install"
    },
    "dependencies": {
        "cors": "^2.8.5",
        "dotenv": "^16.4.5",
        "express": "^4.19.2",
        "helmet": "^7.1.0",
        "mongoose": "^8.3.1"
    },
    "devDependencies": {
        "@types/node": "^20.12.7",
        "typescript": "^5.4.5"
    }
}